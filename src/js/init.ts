export const checkVersion = (newVersion:string) => {
    // const version = localStorage.getItem('version');
    // if (version !== newVersion) {
    //     alert('Es gab Änderungen im Source Code. Der Cache wird gelöscht und die Seite neu geladen.');
    //     localStorage.setItem('version', newVersion);
    //     //Delete cache
    //     //@ts-ignore
    //     window.location = window.location.href+'?eraseCache=true';
        
    // }
}


export const InitTable = () => {
    const InputTyp = document.getElementById('SelectInputTyp') as HTMLSelectElement;
    
};